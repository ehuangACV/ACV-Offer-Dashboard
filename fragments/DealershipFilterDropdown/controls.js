export default {
  isMultiDealer: {
    type: 'boolean',
    label: 'Only shown when account has multiple dealerships (isMultiDealer)',
    default: true
  },
  preSelected: {
    type: 'text',
    label: 'Pre-selected dealerships, comma-separated (preSelected)',
    default: ''
  }
}
